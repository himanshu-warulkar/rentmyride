<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rent My Ride - Listing Details</title>
</head>
<body>
    <h1><%= listing.title %></h1>
    <img src="<%= listing.image %>" alt="<%= listing.title %>" style="width: 400px; height: auto;">
    <p>Price: &#8377; <%= listing.price %></p>
    <p>Description: <%= listing.description %></p>
    <p>Location: <%= listing.location %></p>
    <p>Contact: <%= listing.contact %></p>
    <p>Mileage: <%= listing.mileage %> miles</p>
    <p>Year: <%= listing.year %></p>
    <p>Condition: <%= listing.condition %></p>
    <p>Fuel Type: <%= listing.fuelType %></p>
    <p>Transmission: <%= listing.transmission %></p>
    <p>Seats: <%= listing.seats %></p>
    <p>Cubic Capacity: <%= listing.cubicCapacity %></p>
    <p>Posted on: <%= listing.createdAt.toLocaleDateString() %></p>
    <p>Last updated: <%= listing.updatedAt.toLocaleDateString() %></p>
    <h2>Reviews</h2>
    <ul>
        <% if (listing.reviews && listing.reviews.length > 0) { %>
            <% listing.reviews.forEach(review => { %>
                <li>
                    <strong><%= review.author %>:</strong> <%= review.comment %>
                    <em>Rating: <%= review.rating %>/5</em>
                </li>
            <% }); %>
        <% } else { %>
            <li>No reviews yet.</li>
        <% } %>
    </ul>
    <h2>Add a Review</h2>
    <form action="/listings/<%= listing._id %>/reviews" method="POST">
        <label for="author">Your Name:</label>
        <input type="text" id="author" name="author" required>
        <label for="comment">Your Review:</label>
        <textarea id="comment" name="comment" required></textarea>
        <label for="rating">Rating (1-5):</label>
        <input type="number" id="rating" name="rating" min="1" max="5" required>
        <button type="submit">Submit Review</button>
    </form>
    <hr>
    <h2>Contact the Owner</h2>
    <form action="/listings/<%= listing._id %>/contact" method="POST">
        <label for="message">Your Message:</label>
        <textarea id="message" name="message" required></textarea>
        <button type="submit">Send Message</button>
    </form>
    <hr>
    <h2>Actions</h2>
    <a href="/listings/<%= listing._id %>/edit">Edit Listing</a>
    <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST" style="display:inline;">
        <button type="submit">Delete Listing</button>
    </form>
    <hr>
    <a href="/listings">Back to Listings</a>
    <footer>
        <p>&copy; 2023 Rent My Ride. All rights reserved.</p>
    </footer>
</body> 
</html>